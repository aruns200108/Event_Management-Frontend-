import { CeremonysearchPipe } from './ceremonysearch.pipe';

describe('CeremonysearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CeremonysearchPipe();
    expect(pipe).toBeTruthy();
  });
});
